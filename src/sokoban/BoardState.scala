package sokoban

import sokoban.pieces.Piece
import sokoban.pieces.groupings.PieceGroup

import scala.collection.immutable.HashMap

class BoardState(val map: HashMap[Position, PieceGroup]) {
//  for ((k, v) <- map) {
//    if (v.contains())
//  }
  val boxesTotal = 0
  val goalsTotal = 0
  val boxesOnGoalsCount = 0

}
